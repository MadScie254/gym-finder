# Browser key for Maps JavaScript API (restrict by HTTP referrer in Google Cloud).
NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Server key for Places API New (restrict by IP). Falls back to the public key in local demo.
GOOGLE_MAPS_SERVER_KEY=
